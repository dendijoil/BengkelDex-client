import { Button, Text, Center, Modal, VStack, HStack, Radio, } from "native-base";
import { SafeAreaView } from "react-native-safe-area-context";
import { useState } from "react";

export default function Profile() {
  
  return (
    <SafeAreaView>
      <Text>Profile</Text>
    </SafeAreaView>
  )
};